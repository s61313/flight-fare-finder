# Flight Price Notifier v1

## Build
- Replace the starter screen with a polished dark bilingual landing page at `/`, including the requested three feature cards and footer.
- Add email/password sign-up and sign-in at `/auth`, with clear loading and error states.
- Add a protected `/app` screen that greets the signed-in user by email and shows the requested milestone placeholder.
- Add session-aware navigation and secure sign-out behavior.

## Design
- Use a near-black aviation-inspired interface with violet accents, Inter typography, fine route-grid details, and restrained motion.
- Keep all screens fully usable on mobile and desktop.

## Technical details
- Use Lovable Cloud authentication only and the existing managed route guard.
- Auto-confirm email for this first version, as requested.
- Do not create profiles, subscriptions, RPCs, or any custom data tables.
- Add page-specific metadata and verify public, auth, and protected flows in the preview.
